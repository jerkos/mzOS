language: python

python:
  - "2.7_with_system_site_packages"
# command to install dependencies

before_install:
  - sudo apt-get update
  - sudo apt-get install gcc
  - sudo apt-get install libatlas-dev libatlas-base-dev liblapack-dev gfortran

install:
  - pip install -I numpy==1.9.3 # https://github.com/numpy/numpy/issues/6462
  - pip install scipy
  - pip install scikit-learn
  - pip install .
  - cd mzos/third_party/emass && make
  - pip install coveralls

script:
  - nosetests --with-coverage --cover-package=mzos

after_success:
  - coveralls
